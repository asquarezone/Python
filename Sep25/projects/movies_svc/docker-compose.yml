services:
  db:
    image: postgres:16-alpine
    container_name: movies-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: movies_db
    ports:
      - "5432:5432"
    volumes:
      - movies_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: movies-svc
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@db:5432/movies_db
    ports:
      - "18000:8000"
    # For local dev hot-reload, uncomment:
    # volumes:
    #   - ./src:/app/src

volumes:
  movies_db_data:
